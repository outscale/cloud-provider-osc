# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: "osc-cloud-controller-manager"
    binary: osc-cloud-controller-manager
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
    ldflags:
        - "-s -w {{ if not .IsSnapshot }}-X {{ .ModulePath }}/ccm/utils.version=v{{.Version}}{{ end }}"
    main: ./cmd/osc-cloud-controller-manager
  - id: "osc-labeler"
    binary: osc-labeler
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
    ldflags:
        - "-s -w {{ if not .IsSnapshot }}-X {{ .ModulePath }}/ccm/utils.version=v{{.Version}}{{ end }}"
    main: ./cmd/osc-labeler

changelog:
  use: github-native

release:
  draft: true
  name_template: "Release v{{.Version}}"

dockers_v2:
  - images:
      - "outscale/cloud-provider-osc"
    tags:
      - "v{{ .Version }}"
      - "{{ if .IsSnapshot }}{{ .FullCommit }}{{ end }}"
    platforms:
      - linux/amd64
    labels:
      "org.opencontainers.image.description": "cloud-provider-osc"
      "org.opencontainers.image.created": "{{.Date}}"
      "org.opencontainers.image.name": "{{.ProjectName}}"
      "org.opencontainers.image.revision": "{{.FullCommit}}"
      "org.opencontainers.image.version": "{{.Version}}"
      "org.opencontainers.image.source": "{{.GitURL}}"
    build_args:
      VERSION: "v{{ .Version }}"
